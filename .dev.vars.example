# Local emulation only: `cp .dev.vars.example .dev.vars` then `wrangler pages dev dist`.
# Production values go in the Cloudflare dashboard (Pages project → Settings → Environment variables).
LEAD_EMAIL_TO=shiftify@gmail.com
LEAD_EMAIL_FROM=leads@your-verified-sender.tld
SMTP_HOST=https://your-mail-relay-endpoint.example/send
SMTP_PORT=443
SMTP_USER=api-user
SMTP_PASSWORD=replace-me
