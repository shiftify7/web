# ── Shiftify lead-endpoint secrets (SERVER-SIDE ONLY — never in client code) ──
# Cloudflare Pages: set these in project settings / wrangler secret.
LEAD_EMAIL_TO=shiftify@gmail.com
LEAD_EMAIL_FROM=leads@your-verified-sender.tld
SMTP_HOST=https://your-mail-relay-endpoint.example/send
SMTP_PORT=443
SMTP_USER=api-user
SMTP_PASSWORD=replace-me
